#!/usr/bin/env bash

[[ -n $DISPLAY ]] && export BROWSER='google-chrome'

source ~/bin/local/bash_extras #bash_alias type stuff

#host-specific configs:
ln -sf ~/.host/src/host.${HOSTNAME}/.tmux.conf ~/

# vim: et:ts=4:sw=4
