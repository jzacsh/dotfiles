#!/usr/bin/env bash

[[ -n $DISPLAY ]] && export BROWSER='google-chrome'

export PATH="$PATH:/usr/games/"

source ~/.host/src/distro.goobuntu/bash_extras #bash_alias type stuff

#host-specific configs:
ln -sf ~/.host/src/distro.goobuntu/src/.tmux.conf ~/
source ~/.host/common/zagat

# vim: et:ts=4:sw=4
