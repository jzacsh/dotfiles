#!/usr/bin/env bash
[[ -n $DISPLAY ]] && export BROWSER='chromium' || export BROWSER='w3m'
unalias goh
alias m='mutt'
unset wfls wfon wfnp

ln -sf ~/.host/src/host.blackbox/.Xdefaults ~/

# vim: et:ts=2:sw=2
