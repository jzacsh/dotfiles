#!/usr/bin/env bash
[[ -n $DISPLAY ]] && export BROWSER='google-chrome' || export BROWSER='w3m'
unalias goh
alias m='mutt'
unset wfls wfon wfnp

if is_desktop_env; then
  host_forest_link ~/.host/src/host.blackbox/.Xdefaults ~/
fi

# vim: et:ts=2:sw=2
